var SourceBufferDataQueue = require('../src/js/sourceBuffer/SourceBufferDataQueue.js');

QUnit.test("SourceBufferDataQueue exists", function(assert) {
  assert.ok(SourceBufferDataQueue, "SourceBufferDataQueue does exist!");
});